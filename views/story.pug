extends layout

block content
  main
    .container
      h1 Flickan med svavelstickorna
      if story
        p.lead= story.body
        ul.list
          each link in links
            li
             a(href = link.target_id)=link.description